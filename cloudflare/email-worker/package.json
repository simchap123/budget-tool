{
  "name": "budget-email-ingest",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Email Worker that turns forwarded bank alert emails into budget-tool transactions",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.2.7"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
